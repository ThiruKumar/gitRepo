#!/bin/bash
sudo npm cache clear
npm cache clear
sudo npm install bower -g
sudo npm install grunt -g
sudo npm install karma -g 
sudo npm install grunt-karma -g
sudo npm install grunt-cli -g
sudo npm install yo -g
sudo npm install jasmine-core@* -g
sudo npm install karma-jasmine -g
sudo npm install generator-karma@~0.5.0 -g
sudo npm install angular-mocks -g
sudo npm install generator-go-angular -g
sudo npm install angular-ui-bootstrap -g
sudo npm install fetch-swagger-schema -g

